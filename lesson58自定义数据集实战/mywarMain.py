#自定义数据集实战步骤:#1.加载数据######数据集划分# 皮卡丘:234# 超梦:239# 杰尼龟:233# 小火龙:238# 妙蛙种子:234# train-60%=138;validation-20%=46;test-20%=46# 百度网盘:#继承torch.utils.data.Dataset#_len_数据的长度#__getitem__读取接口# for exampleclass NumbersDataset(Dataset):    def __init__(self, tranining=True):        if tranining:            self.samples = list(range(1, 1001))        else:            self.samples = list(range(1001, 1501))    def __len__(self):        return len(self.samples)    def __getitem__(self, idx):        return self.samples[idx]#####预处理#图片尺寸==>224*224 for ResNet18#数据增强==>旋转,裁剪(辅助性地提升一部分性能)#归一化==>Mean,std#转化为tensor# 详情见pokemon.py#2.建立模型#继承基础类#定义向前图# 详情见resnet.py#3.训练和测试# 详情见train_scratch.py#4.迁移学习#定义:在A任务上训练好一个分类器,在迁移迁移到B上去#二分类# 详情见train_transfer.py#5.小总结